{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["$NODE_ENV"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "dependsOn": ["^dev"]
    },
    "generate": {
      "dependsOn": ["^generate"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    }
  }
}
